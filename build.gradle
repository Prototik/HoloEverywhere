allprojects {
    version = '0.2.16'

    repositories {
        mavenCentral()
    }
}

task('test', type: GradleBuild) {
    dependsOn ':plugin:upload'

    dir = new File(rootProject.rootDir, 'test')
    buildFile = new File(dir, 'build.gradle')
    tasks = ['clean', 'build'] as List<String>
}
